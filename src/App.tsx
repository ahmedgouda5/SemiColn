import { BrowserRouter } from "react-router-dom";
import Routing from "./shared/Routing";

const App = () => {
  return (
    <BrowserRouter>
      <Routing />
    </BrowserRouter>
  );
};

export default App;
